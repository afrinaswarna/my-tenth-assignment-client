import React from 'react';
import HeroSlider from './HeroSlider';

const Banner = () => {
    return (
        <div>
            <HeroSlider></HeroSlider>
        </div>
    );
};

export default Banner;